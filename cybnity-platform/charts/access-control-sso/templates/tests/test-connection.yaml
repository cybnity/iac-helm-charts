apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "access-control-sso.fullname" . }}-test-connection"
  labels:
    {{- include "access-control-sso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "access-control-sso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
